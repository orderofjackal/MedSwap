
{
  "name": "medswap-pro",
  "version": "1.0.0",
  "description": "Hospital overstock exchange platform",
  "main": "index.tsx",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "npx serve .",
    "build": "echo 'No build step required for ESM-native app' && exit 0"
  },
  "dependencies": {},
  "devDependencies": {}
}
